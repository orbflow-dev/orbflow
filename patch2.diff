--- crates/orbflow-worker/src/credential_proxy.rs
+++ crates/orbflow-worker/src/credential_proxy.rs
@@ -220,9 +220,7 @@
         )));
     }

-    if let Ok(ip) = host.parse::<IpAddr>() {
-        if let Some(reason) = is_private_ip(&ip, false) {
-            return Err(OrbflowError::InvalidNodeConfig(format!(
-                "credential proxy blocked request to {reason}: {host}"
-            )));
-        }
-    }
+    if let Some(reason) = host.parse::<IpAddr>().ok().and_then(|ip| is_private_ip(&ip, false)) {
+        return Err(OrbflowError::InvalidNodeConfig(format!(
+            "credential proxy blocked request to {reason}: {host}"
+        )));
+    }

     Ok(parsed)
